import 'package:flutter/material.dart';

//  Image 加载和缓存
class ImageSavior extends StatelessWidget {
  const ImageSavior({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
